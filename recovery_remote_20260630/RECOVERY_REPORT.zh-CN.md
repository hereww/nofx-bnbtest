# NOFX 数据恢复报告 - 2026-06-30

## 当前结论

线上 `/www/wwwroot/nofx/data/data.db` 目前是一个新的小库，不是原始业务库。

当前线上库状态：

- `users`: 1
- `ai_models`: 0
- `exchanges`: 0
- `strategies`: 3
- `traders`: 0

服务器上的 NOFX 容器当前已停止，未继续写入该库。

## 已找到的线索

- 2026-06-15 的备份记录显示，当时旧运行目录是 `/www/wwwroot/nofx-current/data`，数据目录大小约 `7.6G`。
- 当前服务器常规路径下已找不到 `/www/wwwroot/nofx-current`。
- Docker 挂载确认 NOFX 使用宿主机目录 `/www/wwwroot/nofx/data`，没有单独 Docker volume 保存旧库。
- 宝塔/部署备份目录主要保存代码和发布包，发布包构建脚本排除了 `.db`、`.sqlite` 等数据库文件。
- 当前发现的 `/www/wwwroot/nofx-data-snapshots/20260630-215624/data/data.db` 也是小库，不是原始业务库。
- `/root/nofx/data/data.db` 是更早的旧小库，只有旧模型/策略，没有这次丢失的 6 月交易员数据。

## 已恢复出的旧业务数据

本地候选库：

`recovery_remote_20260630/data.recovered-candidate-v1.db`

完整性：

- `PRAGMA integrity_check`: `ok`
- 所有 trader 外键引用已补齐
- 所有 trader 均强制设置为停止状态，避免恢复后误启动实盘

恢复出的旧用户：

- email: `hereww@qq.com`
- user id: `9a0bb67c-7822-4007-b6de-7fbb8819af4d`
- 登录密码哈希使用当前小库中的现有哈希迁移，避免明文处理密码

恢复出的交易员：

- `测试交易`
  - 初始资金：`5009.40795613`
  - 周期：`60` 分钟
  - 杠杆：`10 / 5`
  - 创建时间：`2026-06-21 17:04:56`
  - 更新时间：`2026-06-28 01:35:47`
- `etc交易员`
  - 初始资金：`4921.31330325`
  - 周期：`15` 分钟
  - 杠杆：`10 / 5`
  - 创建时间：`2026-06-23 02:34:20`
  - 更新时间：`2026-06-30 04:01:49`
- `量化交易主账户以太坊`
  - 初始资金：`100`
  - 周期：`3` 分钟
  - 杠杆：`10 / 5`
  - 创建时间：`2026-06-24 14:54:55`
  - 更新时间：`2026-06-30 11:02:37`

恢复出的完整策略：

- `币安模拟盘主流币低风险策略`
- `新策略`
- `自定义代币地址监控策略`

为了保持交易员引用完整，候选库中还创建了 2 条明确标注的占位策略：

- `恢复占位策略-etc交易员`
- `恢复占位策略-量化交易主账户以太坊`

这两条不是完整原始策略记录，只是为了让交易员配置能打开，后续需要人工核对。

## 未完整恢复的数据

以下内容目前没有找到完整原始记录：

- `ai_models` 的完整旧记录
- `exchanges` 的完整旧记录
- Binance API Key / Secret 等交易所凭证
- 原始 `07e87e1d-c966-487e-b396-0ef0dc5ece2a` 策略配置
- 原始 `0585063f-1c80-4e8b-bf54-7c4e8609817c` 策略配置
- `decision_records`
- `trader_equity_snapshots`
- `trader_positions`
- `trader_orders`
- `trader_fills`

候选库中模型和交易所配置均为禁用占位，不包含凭证，避免误交易。

## 安全边界

目前未执行以下动作：

- 未替换线上 `/www/wwwroot/nofx/data/data.db`
- 未删除线上数据目录
- 未启动 NOFX 容器
- 未执行 `docker compose down -v`
- 未删除恢复候选文件

## 建议下一步

1. 先把候选库作为单独文件上传到服务器，例如：
   `/www/wwwroot/nofx/data/data.db.recovered-candidate-v1`
2. 在服务器上对候选库执行只读校验。
3. 用户确认后，再备份当前小库并替换 `data.db`。
4. 替换后仍保持交易员停止状态，先登录前端检查：
   - 3 个交易员是否出现
   - 策略是否可打开
   - 模型配置是否需要重填
   - 交易所配置是否需要重填
5. 模型和交易所凭证重填完成后，再手动启动交易员。

## 高风险提醒

不要直接把当前候选库当成完整恢复版启动实盘。它可以恢复界面和大部分交易员结构，但模型、交易所凭证和两条策略配置仍需人工确认。
